import React from 'react';

// Placeholder for Widget Configuration Panel
const TableConfig: React.FC = () => {
  return (
    <div className="p-4">
      <h3 className="text-lg font-semibold mb-2">Table Configuration</h3>
      <p className="text-sm text-text-muted">Settings to configure sort keys, pagination, and columns will go here.</p>
    </div>
  );
};

export default TableConfig;
